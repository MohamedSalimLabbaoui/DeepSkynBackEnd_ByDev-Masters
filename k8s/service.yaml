apiVersion: v1
kind: Service
metadata:
  name: deepskyn-backend-service
  namespace: deepskyn
  labels:
    app: deepskyn-backend
spec:
  type: ClusterIP
  selector:
    app: deepskyn-backend
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
